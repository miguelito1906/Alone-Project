using UnityEngine;
using System.Collections;

public class MP5Flashlight : MonoBehaviour {


	// Update is called once per frame
	void Update () 
	{
		if (Input.GetKeyDown (KeyCode.L)) 
		{
			if(light.enabled == true)
				light.enabled = false;
			else
				light.enabled = true;
		}
	}
}
